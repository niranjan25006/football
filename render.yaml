services:
  # Backend Web Service
  - type: web
    name: football-backend
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://niranjan25006_db_user:qVaJ5AKRVDRYQaJq@football.2g1apad.mongodb.net/football_club?retryWrites=true&w=majority
      - key: JWT_SECRET
        value: mysecretkey123
      - key: PORT
        value: 5000

  # Frontend Static Site
  - type: web
    name: football-frontend
    runtime: static
    rootDir: client
    staticPublishPath: .
    plan: free
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
